[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "omnibot"
version = "0.1.0"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["03-development/src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
